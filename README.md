Before Android N, a `textColor` defined in a `textAppearance` on a Colored button would work:

![](pre-N.png)

Android N and later, `textColor` defined in a `textAppearance` on a Colored button do not work:

![](post-N.png)